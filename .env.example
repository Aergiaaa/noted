# Copy to .env (never commit .env). Generate secrets with:
#   openssl rand -base64 32
APP_ENV=dev
APP_ORIGIN=http://localhost:5173
TZ=UTC
DATA_PATH=/data/noted.db
CURRENCY_DEFAULT=USD
SETUP_TOKEN=
TOTP_ENC_KEY=
TRUSTED_PROXIES=
